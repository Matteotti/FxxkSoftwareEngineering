19 病例类(case)

(1)主键：
    只对前端开放查询接口，剩下的交给后端处理
(2)病人主键：
    无需格式检测
(3)医生主键：
    无需格式检测
(4)病例日期：
    在病例创建的时候后端自动生成
(5)对白主键：
    对白生成之后后端自动添加
(6)缴费账单：
    账单生成之后后端自动添加
(7)检测结果：
    检测结果生成之后后端自动添加
(8)诊断结果：
    医生填写病例单的时候加入进去
