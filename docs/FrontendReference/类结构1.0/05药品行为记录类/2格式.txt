05 药品行为记录类(drugaction)

(1)主键：
    只对前端开放查询接口，剩下的交给后端处理
(2)药品主键：
    根据药品表来选择对应的主键即可
(3)行为描述：
    药品上架、药品下架、更改药品信息（管理端）
    开方（医生端）  购买（病人端）
(4)行为主体：
    病人名称/医生名称/管理名称
(5)行为时间：
    只对前端开放查询接口，剩下的交给后端处理