16 医生当前病人队列类(dopaqueue)

(1)主键：
    只对前端开放查询接口，剩下的交给后端处理
(2)医生主键：
    无需格式检测
(3)问诊日期：
    后端自动生成
(4)上下午：
    后端自动生成
(5)存储文件：
    文件命名规范
        <主键_日期_上下午_名称.txt>
    文件路径
        scr/main/java/resources/data/queue/file.txt