17 对话记录类(dialogue)

(1)主键：
    只对前端开放查询接口，剩下的交给后端处理
(2)医生主键：
    无需格式检测
(3)病人主键：
    无需格式检测
(4)存储文件：
    文件命名规范
        <主键_医生名称_病人名称.txt>
    文件路径
        scr/main/java/resources/data/dialog/file.txt